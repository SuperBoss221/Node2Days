เอาสิ่งที่แก้ไขเข้า git ในเครื่องตัวเองก่อน
git add . 

เก็บประวัติว่าทำอะไร
git commit -m "message"

ส่งสิ่งที่แก้ไขไปเซิฟเวอร์
git push

ตรวจสถานะว่าแก้อะไรไปบ้างก่อนทำการ add .
git status

ดึงโค้ดล่าสุดมาไว้ในคอมตัวเอง
git pull

git push --force (อย่าใช้บ่อยอันตราย)